<%= form_for @comment do |f| %>
<%= f.label :content, "Put Comment Here" %><br>
<%= f.fields_for :user do |ff| %>
<%= f.label :username %>
<%= f.text_field :username %>
<% end%>
<%= f.text_area :content %><br> Username:
<%= f.collection_select :user_id, User.all, :id, :username%><br>Post:
<%= f.collection_select :post_id, Post.all, :id, :title %><br>
<%= f.submit %>
<% end%>